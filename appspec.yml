version: 0.0
os: linux
files:
  - source: /index.html
    destination: /var/www/html/
hooks:
  BeforeInstall:
    - location: service httpd stop
      timeout: 300
      runas: root
  AfterInstall:
    - location: service httpd start
      timeout: 300
      runas: root
